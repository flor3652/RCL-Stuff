# If using a computer that has trouble downloading from CRAN, try removing the "#" from in front of the following code and running again
# chooseCRANmirror(ind=which(getCRANmirrors()[,1] == "USA (CO)"),useHTTPS=FALSE)
if(!require(swirl)) install.packages("swirl")
library(swirl)
swirl()
